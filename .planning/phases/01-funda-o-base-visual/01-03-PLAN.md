---
phase: 01-funda-o-base-visual
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - src/layouts/BaseLayout.astro
  - src/components/Header.astro
  - src/components/Footer.astro
  - src/components/WhatsAppButton.astro
  - src/pages/index.astro
  - src/pages/solucoes.astro
  - src/pages/suporte.astro
  - src/pages/contato.astro
  - src/styles/global.css
  - src/components/ThemeSwitcher.astro
autonomous: true
requirements: [FND-05, FND-06, FND-08]

must_haves:
  truths:
    - "As 4 páginas usam o BaseLayout e exibem Header (com logo), Footer e botão WhatsApp flutuante"
    - "A navegação do Header (Home/Soluções/Suporte/Contato) funciona em todas as páginas"
    - "Em telas estreitas o Header vira um menu hambúrguer que abre/fecha"
    - "O botão WhatsApp flutuante aponta para wa.me com o número placeholder documentado"
    - "O Footer mostra o telefone 51 3022-3066 e o endereço de Porto Alegre"
    - "O ThemeSwitcher aparece no Header e troca a cor primária ao vivo em qualquer página"
    - "O layout não quebra em mobile, tablet e desktop (sem overflow horizontal)"
    - "O logo do brand é exibido no Header (versão coerente com o fundo), com a troca claro/escuro documentada para uso conforme o tema"
  artifacts:
    - path: "src/layouts/BaseLayout.astro"
      provides: "Layout raiz: <head> com SEO placeholder/favicon/fontes, data-theme + anti-flash, Header/Footer/WhatsApp via slot"
      min_lines: 40
      contains: "favicon"
    - path: "src/components/Header.astro"
      provides: "Header com nav, logo, menu mobile hambúrguer e ThemeSwitcher"
      min_lines: 40
    - path: "src/components/Footer.astro"
      provides: "Footer com telefone e endereço reais"
      contains: "3022-3066"
    - path: "src/components/WhatsAppButton.astro"
      provides: "Botão WhatsApp flutuante (wa.me placeholder documentado)"
      contains: "wa.me"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/layouts/BaseLayout.astro"
      via: "import e wrap do conteúdo no layout"
      pattern: "BaseLayout"
    - from: "src/layouts/BaseLayout.astro"
      to: "src/components/Header.astro"
      via: "import e render do Header"
      pattern: "Header"
    - from: "src/components/Header.astro"
      to: "public/brand/logo-horizontal.png"
      via: "img do logo no header conforme tema"
      pattern: "/brand/logo"
    - from: "src/components/Header.astro"
      to: "src/components/ThemeSwitcher.astro"
      via: "render do seletor de tema no header"
      pattern: "ThemeSwitcher"
---

<objective>
Montar o layout base responsivo do site: BaseLayout.astro (head/SEO placeholder/favicon/fontes/data-theme + anti-flash), Header.astro (nav + logo que combina com o tema + menu mobile hambúrguer + ThemeSwitcher), Footer.astro (telefone + endereço reais) e WhatsAppButton.astro (flutuante). Aplicar o layout às 4 páginas e garantir responsividade mobile/tablet/desktop.

Purpose: Fechar a Fase 1 entregando o "shell" navegável, temável e responsivo — Header/Footer/WhatsApp em todas as páginas (FND-05), 100% responsivo (FND-06) e o ThemeSwitcher ao vivo no Header com anti-flash global (FND-08).
Output: Shell completo aplicado às 4 páginas; conteúdo real é Fase 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-funda-o-base-visual/01-CONTEXT.md
@.planning/phases/01-funda-o-base-visual/01-01-SUMMARY.md
@.planning/phases/01-funda-o-base-visual/01-02-SUMMARY.md

# Depende de 01-01 (projeto Astro, páginas placeholder, public/brand/ com logos+favicon)
# e de 01-02 (tokens.css, global.css, primitivos Container/Section/Button/Card, ThemeSwitcher).
# Decisões pinadas (NÃO reabrir):
# - data-theme no <html>; persistência localStorage; anti-flash antes do paint.
# - Mobile-first; breakpoints simples ~640/768/1024; menu mobile hambúrguer funcional.
# - Logo do header acompanha o tema (ex.: logo Branco em fundos escuros).

<known_facts>
Contato real (de PROJECT.md): Fone 51 3022-3066 · Av. Doutor Nilo Peçanha 3238, 2º andar,
  Chácara das Pedras, Porto Alegre/RS, CEP 91330-001.
WhatsApp: número ainda não fornecido — usar placeholder wa.me/55XXXXXXXXXXX e DOCUMENTAR.
Nav do site: Home (/), Soluções (/solucoes), Suporte (/suporte), Contato (/contato).
Assets gerados por 01-01 em public/brand/: favicon.png, logo-horizontal.png (ocean),
  logo-horizontal-branco.png (para fundos escuros), logo.svg, e subpastas por cor.
De 01-02: tokens semânticos, primitivos (Container/Section/Button/Card), ThemeSwitcher.astro,
  chave localStorage do tema e o snippet anti-flash is:inline a ser hoistado para o <head>.
</known_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BaseLayout.astro com head/SEO placeholder, favicon, fontes, data-theme e anti-flash</name>
  <files>src/layouts/BaseLayout.astro, src/styles/global.css</files>
  <action>
    Criar src/layouts/BaseLayout.astro como o layout raiz. Estrutura:
    - <html lang="pt-BR"> com o data-theme inicial = "ocean" (default). No <head>, ANTES de qualquer render, incluir o snippet anti-flash <script is:inline>: lê localStorage (chave do tema) e, se houver, aplica document.documentElement.setAttribute('data-theme', ...) imediatamente — garante zero flash em TODAS as páginas. Este BaseLayout passa a ser o ponto CANÔNICO do anti-flash. Por isso, REMOVER (ou comentar) o <script is:inline> redundante de src/components/ThemeSwitcher.astro para evitar execução duplicada — o ThemeSwitcher mantém apenas seu <script> client-side (não inline) que trata cliques, persistência em localStorage e estado ativo dos botões.
    - <head>: <meta charset>, viewport, <title> e <meta name="description"> recebidos via props (props `title` e `description` com defaults placeholder em pt-BR — SEO real é Fase 3), <link rel="icon" href="/brand/favicon.png">. Importar src/styles/tokens.css e src/styles/global.css (via import no frontmatter ou <link>) para que os tokens e a fonte estejam disponíveis em todas as páginas.
    - <body>: render do <Header />, depois <slot /> (conteúdo da página), depois <Footer />, e o <WhatsAppButton /> flutuante (fora do fluxo). Importar Header, Footer e WhatsAppButton (criados na Task 2).
    - Aceitar props title/description e repassar valores sensatos.

    Garantir que a importação global de estilos não duplique a fonte (a fonte é importada em global.css conforme 01-02). Se necessário, ajustar global.css para incluir utilidades responsivas mínimas (ex.: classe .visually-hidden para acessibilidade do menu, e prevenção de overflow-x no body) — manter enxuto.
  </action>
  <verify>
    <automated>test -f src/layouts/BaseLayout.astro && grep -q 'is:inline' src/layouts/BaseLayout.astro && grep -q '/brand/favicon.png' src/layouts/BaseLayout.astro && test -f public/brand/favicon.png && grep -q 'lang="pt-BR"' src/layouts/BaseLayout.astro && grep -qiE 'tokens.css|global.css' src/layouts/BaseLayout.astro && echo LAYOUT_OK</automated>
  </verify>
  <done>BaseLayout.astro define <html lang="pt-BR"> com anti-flash is:inline no <head>, favicon do brand, title/description via props, tokens+global importados, e renderiza Header/slot/Footer/WhatsAppButton.</done>
</task>

<task type="auto">
  <name>Task 2: Header (nav + logo temático + menu mobile + ThemeSwitcher), Footer e WhatsAppButton</name>
  <files>src/components/Header.astro, src/components/Footer.astro, src/components/WhatsAppButton.astro</files>
  <action>
    Header.astro:
    - Logo à esquerda usando os assets de public/brand/ (de 01-01). Para "combinar com o tema": como o header tem fundo claro por padrão, usar /brand/logo-horizontal.png (ocean). Se optar por um header de fundo escuro (tone dark), trocar para /brand/logo-horizontal-branco.png. Implementar de forma simples — se o header for claro, basta o logo colorido; deixe comentado/preparado o swap para fundo escuro. O logo deve linkar para "/".
    - Navegação: links Home (/), Soluções (/solucoes), Suporte (/suporte), Contato (/contato). Marcar o link ativo (comparar Astro.url.pathname) com estilo via tokens.
    - ThemeSwitcher: importar e renderizar <ThemeSwitcher /> (de 01-02) no header (ex.: à direita), visível em desktop e acessível no menu mobile.
    - Menu mobile hambúrguer: abaixo do breakpoint (~768px), esconder a nav inline e mostrar um botão hambúrguer (aria-expanded, aria-controls) que abre/fecha um painel com os links + o ThemeSwitcher. JS vanilla no <script> do componente (toggle de classe/atributo). Fechar ao clicar num link. Acessível por teclado.
    - <style> escopado usando tokens; sem hex hardcoded.

    Footer.astro:
    - Exibir telefone "51 3022-3066" (como <a href="tel:+555130223066">) e o endereço completo: "Av. Doutor Nilo Peçanha 3238, 2º andar, Chácara das Pedras, Porto Alegre/RS, CEP 91330-001". Pode repetir a navegação e um logo reduzido (/brand/marca-512 da pasta apropriada ou logo-horizontal). Tom premium, fundo possivelmente navy (tone dark) usando token — se escuro, usar logo branco. <style> via tokens.

    WhatsAppButton.astro:
    - Botão/link flutuante fixo (position fixed, canto inferior direito, acima do conteúdo, fora do fluxo) presente via BaseLayout em todas as páginas. href = "https://wa.me/55XXXXXXXXXXX" — NÚMERO PLACEHOLDER. Adicionar comentário Astro proeminente explicando que 55XXXXXXXXXXX deve ser trocado pelo número real (celular comercial) quando o usuário fornecer, e que o telefone fixo do site é 51 3022-3066. Incluir aria-label "Falar no WhatsApp", target _blank, rel noopener. Ícone simples (SVG inline ou caractere) e estilo via tokens (cor de destaque). Não usar libs externas.
  </action>
  <verify>
    <automated>for f in Header Footer WhatsAppButton; do test -f "src/components/$f.astro" || { echo "MISSING $f"; exit 1; }; done && grep -q '/solucoes' src/components/Header.astro && grep -q 'ThemeSwitcher' src/components/Header.astro && grep -qiE 'hamburg|aria-expanded|menu' src/components/Header.astro && grep -q '/brand/logo' src/components/Header.astro && grep -q '3022-3066' src/components/Footer.astro && grep -qi 'Porto Alegre' src/components/Footer.astro && grep -q 'wa.me/55XXXXXXXXXXX' src/components/WhatsAppButton.astro && ! grep -RniE '#[0-9a-f]{3,6}' src/components/Header.astro src/components/Footer.astro src/components/WhatsAppButton.astro && echo COMPONENTS_OK</automated>
  </verify>
  <done>Header tem logo do brand, nav com link ativo, ThemeSwitcher e menu hambúrguer funcional; Footer tem telefone 51 3022-3066 e endereço de Porto Alegre; WhatsAppButton é flutuante com wa.me placeholder documentado. Nenhum hex hardcoded.</done>
</task>

<task type="auto">
  <name>Task 3: Aplicar BaseLayout às 4 páginas e garantir responsividade mobile/tablet/desktop</name>
  <files>src/pages/index.astro, src/pages/solucoes.astro, src/pages/suporte.astro, src/pages/contato.astro</files>
  <action>
    Reescrever as 4 páginas (index, solucoes, suporte, contato) para que cada uma importe e use BaseLayout.astro, passando title/description placeholder coerentes em pt-BR (ex.: title "KLIT — Início", "KLIT — Soluções", etc.). O conteúdo de cada página continua sendo ESQUELETO desta fase: usar o primitivo <Section> + <Container> (de 01-02) com um <h1> e um parágrafo curto indicando do que a página tratará na Fase 2. Remover a navegação manual inline que existia nos placeholders de 01-01 (agora a navegação vem do Header do layout). Manter pt-BR.

    Responsividade (FND-06): revisar e garantir, com os <style> dos componentes e/ou global.css, que NÃO há overflow horizontal e que a página se adapta em mobile (~375px), tablet (~768px) e desktop (~1280px): Container com padding lateral e max-width; nav inline some e hambúrguer aparece abaixo de ~768px; WhatsApp flutuante não cobre conteúdo crítico; tipografia legível em mobile. Ajustar breakpoints (~640/768/1024) onde necessário. Validar no build e descrever, no SUMMARY, os pontos checados manualmente no dev server.
  </action>
  <verify>
    <automated>for p in index solucoes suporte contato; do grep -q 'BaseLayout' "src/pages/$p.astro" || { echo "PAGE $p sem layout"; exit 1; }; done && npx astro build && test -f dist/index.html && test -f dist/solucoes/index.html && test -f dist/suporte/index.html && test -f dist/contato/index.html && echo PAGES_OK</automated>
  </verify>
  <done>As 4 páginas usam BaseLayout (Header/Footer/WhatsApp aparecem em todas), build passa, e o layout é responsivo (nav vira hambúrguer no mobile, sem overflow horizontal).</done>
</task>

</tasks>

<verification>
1. `npm run dev`: as 4 páginas mostram Header (logo + nav + ThemeSwitcher), Footer (51 3022-3066 + endereço) e botão WhatsApp flutuante.
2. Navegar entre Home/Soluções/Suporte/Contato pelo Header funciona; link ativo destacado.
3. Reduzir a janela < 768px: nav vira hambúrguer que abre/fecha; sem overflow horizontal em 375/768/1280.
4. Clicar num swatch do ThemeSwitcher troca a cor primária ao vivo em qualquer página; recarregar mantém o tema sem flash.
5. Botão WhatsApp aponta para wa.me/55XXXXXXXXXXX (placeholder documentado).
6. `npm run build` passa.
</verification>

<success_criteria>
- Header com navegação + Footer + botão WhatsApp flutuante em todas as páginas — FND-05.
- Estrutura 100% responsiva (mobile/tablet/desktop), menu hambúrguer funcional — FND-06.
- ThemeSwitcher ao vivo no Header, persistente e sem flash em todas as páginas — FND-08.
- Logo do header combina com o tema; favicon do brand aplicado.
</success_criteria>

<output>
Criar `.planning/phases/01-funda-o-base-visual/01-03-SUMMARY.md` ao concluir, registrando: props do BaseLayout, breakpoints usados, comportamento do menu mobile, qual logo é usado conforme o tema/fundo, e a localização do placeholder do número de WhatsApp para troca na Fase 3.
</output>
