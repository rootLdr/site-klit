---
phase: 01-funda-o-base-visual
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/styles/tokens.css
  - src/styles/global.css
  - src/components/Container.astro
  - src/components/Section.astro
  - src/components/Button.astro
  - src/components/Card.astro
  - src/components/ThemeSwitcher.astro
  - package.json
  - package-lock.json
autonomous: true
requirements: [FND-03, FND-04, FND-08]

must_haves:
  truths:
    - "Editar um valor em src/styles/tokens.css (ex.: --color-primary do tema ocean) muda a aparência do site inteiro"
    - "O ThemeSwitcher é um controle visível que troca a cor primária (ocean/navy/sky/soft) com 1 clique, ao vivo"
    - "A escolha de tema persiste em localStorage e é reaplicada ao recarregar a página sem flash de cor errada"
    - "Os componentes Button, Card, Section e Container renderizam usando os tokens (sem cores hardcoded)"
    - "Uma fonte sans moderna está carregada e aplicada como tipografia base"
  artifacts:
    - path: "src/styles/tokens.css"
      provides: "Design tokens: paleta KLIT + temas por data-theme + espaçamento/raio/sombra/tipografia"
      contains: "data-theme"
      min_lines: 50
    - path: "src/styles/global.css"
      provides: "Reset, tipografia base, estilos globais usando os tokens"
      min_lines: 30
    - path: "src/components/Button.astro"
      provides: "Botão primitivo (variantes primária/secundária) via tokens"
    - path: "src/components/Card.astro"
      provides: "Card primitivo via tokens"
    - path: "src/components/Section.astro"
      provides: "Wrapper de seção com espaçamento vertical via tokens"
    - path: "src/components/Container.astro"
      provides: "Container de largura máxima centralizado"
    - path: "src/components/ThemeSwitcher.astro"
      provides: "Seletor de tema runtime + persistência localStorage + anti-flash (FND-08)"
      min_lines: 40
      contains: "localStorage"
  key_links:
    - from: "src/components/ThemeSwitcher.astro"
      to: "src/styles/tokens.css"
      via: "atributo data-theme no <html> aciona os blocos de tema"
      pattern: "data-theme"
    - from: "src/components/ThemeSwitcher.astro"
      to: "localStorage"
      via: "persistência da escolha de tema"
      pattern: "localStorage"
    - from: "src/components/Button.astro"
      to: "src/styles/tokens.css"
      via: "var(--color-primary) e demais tokens"
      pattern: "var\\(--color"
---

<objective>
Definir o design system premium da KLIT: tokens em CSS custom properties (paleta oficial + temas por `data-theme`), estilos globais (reset + tipografia), os 4 componentes primitivos (Button, Card, Section, Container) e o ThemeSwitcher que troca a cor primária em runtime com persistência e sem flash.

Purpose: Entregar um sistema visual trocável por arquivo único (FND-03), peças reutilizáveis (FND-04) e o seletor de tema ao vivo que é critério-chave da fase (FND-08).
Output: tokens.css + global.css importáveis, 4 primitivos .astro e ThemeSwitcher.astro funcional — base para o layout do plano 01-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-funda-o-base-visual/01-CONTEXT.md
@.planning/phases/01-funda-o-base-visual/01-01-SUMMARY.md

# Depende de 01-01: o projeto Astro, src/styles/ e src/components/ já existem (vazios).
# Decisões pinadas que governam este plano (NÃO reabrir):
# - CSS vanilla com custom properties; SEM Tailwind.
# - Temas trocam a COR PRIMÁRIA entre ocean(default)/navy/sky/soft via data-theme no <html>.
# - Persistir em localStorage; aplicar antes do paint (anti-flash).

<brand_palette>
Paleta oficial (de 01-CONTEXT.md). Definir como tokens em tokens.css:
  Ocean Blue (primária default) = #005A91
  Navy Dark                     = #1B3A5C
  Sky Blue (acento)             = #81D2F1
  Soft Blue                     = #A3C5F1
  Preto (texto)                 = #000000
  Branco                        = #FFFFFF
Temas (data-theme) trocam --color-primary para o respectivo hex:
  ocean(default)->#005A91, navy->#1B3A5C, sky->#81D2F1, soft->#A3C5F1
</brand_palette>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tokens, temas e estilos globais (tipografia + reset)</name>
  <files>src/styles/tokens.css, src/styles/global.css, package.json, package-lock.json</files>
  <action>
    Criar src/styles/tokens.css com TODOS os design tokens como CSS custom properties em :root: a paleta de marca (--color-ocean, --color-navy, --color-sky, --color-soft, --color-black, --color-white), tokens semânticos (--color-primary, --color-accent, --color-text, --color-bg, --color-surface, --color-muted), escala de espaçamento (--space-1..--space-8 ou similar), raios (--radius-sm/md/lg), sombras (--shadow-sm/md/lg), e tokens de tipografia (--font-sans, --font-size-base, escala de tamanhos, line-heights, font-weights). O default (:root, equivalente a data-theme="ocean") define --color-primary: var(--color-ocean). Em seguida, blocos por tema que SOBRESCREVEM apenas --color-primary (e --color-accent se fizer sentido): `[data-theme="navy"] { --color-primary: var(--color-navy); }`, idem sky e soft. Garantir que mudar um único valor de token propaga para todo o site (objetivo FND-03 / success criteria #2).

    Escolher e instalar UMA fonte sans moderna via @fontsource (decisão pinada sugere Inter ou Plus Jakarta Sans — escolher uma; Inter é a opção sensata e leve). Rodar `npm install @fontsource/<fonte>`. O import da fonte pode ser feito em global.css (ou no BaseLayout em 01-03 — mas centralize aqui via global.css importando @fontsource). Definir --font-sans para usar essa fonte com fallback de sistema.

    Criar src/styles/global.css que: importa a fonte @fontsource escolhida; aplica um reset moderno enxuto (box-sizing border-box, margin 0, img/picture block max-width 100%, etc.); define estilos base no body usando tokens (font-family var(--font-sans), color var(--color-text), background var(--color-bg)); estilos base de tipografia (h1-h6, p, a, listas) consistentes com tom premium (bastante respiro/line-height). NÃO usar cores hardcoded — sempre var(--token).
  </action>
  <verify>
    <automated>test -f src/styles/tokens.css && test -f src/styles/global.css && grep -q 'data-theme="navy"' src/styles/tokens.css && grep -q 'data-theme="sky"' src/styles/tokens.css && grep -q 'data-theme="soft"' src/styles/tokens.css && grep -q -- '--color-primary' src/styles/tokens.css && grep -qi '@fontsource' src/styles/global.css && grep -rq '@fontsource' package.json && echo TOKENS_OK</automated>
  </verify>
  <done>tokens.css define a paleta + tokens semânticos/espaço/raio/sombra/tipografia e os 4 temas via data-theme sobrescrevendo --color-primary; global.css tem reset + tipografia base via tokens; fonte @fontsource instalada e aplicada.</done>
</task>

<task type="auto">
  <name>Task 2: Componentes primitivos — Container, Section, Button, Card</name>
  <files>src/components/Container.astro, src/components/Section.astro, src/components/Button.astro, src/components/Card.astro</files>
  <action>
    Criar os 4 primitivos .astro, cada um com <style> escopado usando exclusivamente os tokens (var(--...)) — zero cor hardcoded.

    - Container.astro: wrapper de largura máxima (ex.: --container-max ~1200px) centralizado com padding lateral responsivo. Aceita <slot/>. Prop opcional `size` (sm/md/lg) se trivial; senão um tamanho padrão.
    - Section.astro: wrapper semântico <section> com espaçamento vertical generoso (padding via --space-*), tom premium (respiro). Props opcionais: `tone` ("default" | "dark") onde dark usa fundo navy e texto claro (útil para 01-03/Fase 2). Aceita <slot/>.
    - Button.astro: renderiza <a> ou <button> conforme prop `href`. Props: `variant` ("primary" default | "secondary" | "ghost"), `href`. primary usa background var(--color-primary) + texto branco; secondary/ghost variações via tokens. Estados hover/focus visíveis (acessibilidade básica). Como o tema troca --color-primary em runtime, o botão primário MUDA de cor automaticamente ao trocar o tema — confirme que não há cor fixa.
    - Card.astro: superfície com background var(--color-surface)/branco, --radius e --shadow dos tokens, padding via --space-*. Aceita <slot/>. Prop opcional para título.

    Manter tudo simples e bem comentado em pt-BR. Estes primitivos são consumidos por 01-03 (layout) e pela Fase 2 (conteúdo).
  </action>
  <verify>
    <automated>for f in Container Section Button Card; do test -f "src/components/$f.astro" || { echo "MISSING $f"; exit 1; }; done && ! grep -RniE '#[0-9a-f]{3,6}' src/components/*.astro && grep -q 'var(--color-primary)' src/components/Button.astro && npx astro build && echo PRIMITIVES_OK</automated>
  </verify>
  <done>Container, Section, Button e Card existem, usam apenas tokens (nenhum hex hardcoded nos componentes), e o build passa. Button primário usa var(--color-primary).</done>
</task>

<task type="auto">
  <name>Task 3: ThemeSwitcher — troca de cor primária em runtime, persistente e sem flash (FND-08)</name>
  <files>src/components/ThemeSwitcher.astro</files>
  <action>
    Criar src/components/ThemeSwitcher.astro implementando o seletor de tema (FND-08). Requisitos exatos:

    - Controle VISÍVEL: um grupo de 4 botões/swatches, um por tema — ocean (default), navy, sky, soft — cada swatch exibindo a cor do tema (use o hex do token correspondente como background do swatch para o usuário reconhecer). Acessível: cada botão com aria-label (ex.: "Tema Ocean Blue") e estado pressionado (aria-pressed) marcando o tema ativo. Foco por teclado visível.
    - Ação de 1 clique: ao clicar num swatch, setar document.documentElement.setAttribute('data-theme', <tema>) — o que reaplica --color-primary instantaneamente via tokens.css (success criteria #3: "ao vivo"). Atualizar o estado aria-pressed dos botões.
    - Persistência: salvar o tema escolhido em localStorage (chave ex.: "klit-theme"). Ao montar o componente (client-side <script>), ler o localStorage e aplicar.
    - Anti-flash: incluir NESTE componente um <script is:inline> (inline, sem type=module, para rodar antes do paint) que lê localStorage e aplica data-theme no <html> imediatamente. Documentar no SUMMARY que, idealmente, esse snippet inline deve ficar no <head> do BaseLayout (01-03) para garantir zero flash em todas as páginas — mas deixar o componente funcional por si só. (01-03 incluirá o switcher no Header e pode hoistar o snippet anti-flash para o <head>.)
    - <style> escopado usando tokens; visual discreto e premium. Sem dependências externas; vanilla JS no <script> do componente.
  </action>
  <verify>
    <automated>test -f src/components/ThemeSwitcher.astro && grep -q 'data-theme' src/components/ThemeSwitcher.astro && grep -q 'localStorage' src/components/ThemeSwitcher.astro && grep -q 'is:inline' src/components/ThemeSwitcher.astro && grep -qE 'navy|sky|soft|ocean' src/components/ThemeSwitcher.astro && npx astro build && echo SWITCHER_OK</automated>
  </verify>
  <done>ThemeSwitcher.astro renderiza 4 swatches (ocean/navy/sky/soft); clicar troca data-theme no <html> ao vivo; escolha persiste em localStorage e é reaplicada via snippet is:inline (anti-flash); build passa. Verificação visual ao vivo é confirmada quando 01-03 monta o switcher no Header.</done>
</task>

</tasks>

<verification>
1. Editar --color-primary do tema ocean em tokens.css e recarregar — toda a UI primária muda (FND-03 / success #2).
2. Os 4 primitivos compilam e usam apenas tokens (grep não acha hex nos componentes).
3. ThemeSwitcher tem 4 swatches, troca data-theme com 1 clique, persiste em localStorage e tem snippet anti-flash is:inline.
4. `npm run build` passa com todos os componentes.
</verification>

<success_criteria>
- Design system premium definido por tokens trocáveis (cores/tipografia/espaço/grid) — FND-03.
- Componentes base reutilizáveis (botão, card, seção, container) — FND-04.
- Seletor de tema visível troca a cor primária (ocean/navy/sky/soft) com 1 clique, em runtime, persistente e sem flash — FND-08.
</success_criteria>

<output>
Criar `.planning/phases/01-funda-o-base-visual/01-02-SUMMARY.md` ao concluir, registrando: a fonte escolhida, a lista de tokens semânticos disponíveis, a API (props) de cada primitivo, a chave de localStorage do tema, e a recomendação de hoistar o snippet anti-flash para o <head> do BaseLayout em 01-03.
</output>
