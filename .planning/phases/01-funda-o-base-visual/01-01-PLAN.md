---
phase: 01-funda-o-base-visual
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - astro.config.mjs
  - .gitignore
  - .nvmrc
  - tsconfig.json
  - scripts/sync-assets.mjs
  - public/brand/.gitkeep
  - public/brand/README.md
  - src/pages/index.astro
  - src/pages/solucoes.astro
  - src/pages/suporte.astro
  - src/pages/contato.astro
  - src/components/.gitkeep
  - src/layouts/.gitkeep
  - src/styles/.gitkeep
autonomous: true
requirements: [FND-01, FND-02, FND-07]

must_haves:
  truths:
    - "`npm install` completa sem erro e `npm run dev` sobe o site em http://localhost:4321"
    - "As 4 páginas placeholder (/, /solucoes, /suporte, /contato) abrem no navegador sem erro 404"
    - "`npm run assets:sync` copia os logos de assets-input/ para public/brand/ com nomes limpos (sem espaços/bullets)"
    - "O favicon e o logo horizontal ficam disponíveis em public/brand/ após o sync"
    - "public/brand/README.md documenta como trocar o logo e qual comando rodar"
  artifacts:
    - path: "package.json"
      provides: "Projeto Astro com scripts dev/build/preview/assets:sync"
      contains: "assets:sync"
    - path: "astro.config.mjs"
      provides: "Config Astro com output estático"
    - path: "scripts/sync-assets.mjs"
      provides: "Pipeline que leva logos de assets-input/ para public/brand/"
      min_lines: 30
    - path: "src/pages/index.astro"
      provides: "Página home placeholder navegável"
    - path: "src/pages/solucoes.astro"
      provides: "Página soluções placeholder navegável"
    - path: "src/pages/suporte.astro"
      provides: "Página suporte placeholder navegável"
    - path: "src/pages/contato.astro"
      provides: "Página contato placeholder navegável"
    - path: "public/brand/README.md"
      provides: "Documentação de troca de logo (FND-07)"
  key_links:
    - from: "package.json"
      to: "scripts/sync-assets.mjs"
      via: "script npm assets:sync"
      pattern: "assets:sync.*sync-assets"
    - from: "scripts/sync-assets.mjs"
      to: "public/brand/"
      via: "cópia de arquivos de assets-input/logos/Novo Logo 2026/"
      pattern: "public/brand"
---

<objective>
Criar o scaffold do site KLIT em Astro (saída estática), com a estrutura de pastas do projeto, as 4 páginas placeholder navegáveis e o pipeline de assets que leva os logos de `assets-input/` para `public/brand/`.

Esta é a fundação técnica de toda a Fase 1: os planos 01-02 (design system) e 01-03 (layout) constroem em cima desta estrutura.

Purpose: Entregar um projeto que roda localmente com um comando (FND-01) e um caminho documentado e simples para trocar logos/imagens (FND-02, FND-07), sem espalhar assets fora do fluxo.
Output: Projeto Astro instalável e rodável (`npm run dev`), script `assets:sync` funcional, `public/brand/` populado, e 4 páginas esqueleto navegáveis.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-funda-o-base-visual/01-CONTEXT.md

# Decisões pinadas que governam este plano (NÃO reabrir):
# - Astro (última estável) + npm, Node 22, saída estática (astro build -> dist/).
# - CSS vanilla com custom properties; SEM Tailwind.
# - Sem TypeScript estrito obrigatório; manter simples. Sem backend/CMS/DB.

<asset_inventory>
Logos já em disco em `assets-input/logos/Novo Logo 2026/`. Seis pastas de variante por cor,
cada uma com EXATAMENTE estes 5 arquivos (nomes idênticos entre pastas):
  - "Favicon PNG 48 × 48 px.png"
  - "Logo pequeno _ marca reduzida 512 × 512 px.png"
  - "Logo principal horizontal - 1024px.png"
  - "Logo principal para alta resolução - 2048px.png"
  - "Logo vertical_quadrado 1024 × 1024 px.png"

Pastas de variante (nomes EXATOS, incluem bullet "•" e acentos):
  - "Branco"
  - "CLASSICA 01 • Sky Blue - Hex 81D2F1"
  - "CLASSICA 02 • Ocean Blue - 005A91"
  - "Navy Dark - Hex 1B3A5C"
  - "Preto - Hex 000000"
  - "Soft Blue - Hex A3C5F1"

Mais "01 VETOR ORIGINAL.svg" na raiz de "Novo Logo 2026/" (vetor do logo).
(Existem também PNGs soltos com nomes aleatórios na raiz — IGNORAR no sync.)
</asset_inventory>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inicializar projeto Astro estático com estrutura de pastas</name>
  <files>package.json, package-lock.json, astro.config.mjs, .gitignore, .nvmrc, tsconfig.json, src/pages/index.astro, src/pages/solucoes.astro, src/pages/suporte.astro, src/pages/contato.astro, src/components/.gitkeep, src/layouts/.gitkeep, src/styles/.gitkeep, public/brand/.gitkeep</files>
  <action>
    Inicializar um projeto Astro mínimo NA RAIZ do repositório (/home/claude/site-klit já contém .git, .planning, assets-input, docs — NÃO criar subpasta). Use `npm create astro@latest -- --template minimal --no-install --no-git --typescript relaxed --yes .` rodado no diretório atual; se o template criar páginas de exemplo extras (ex.: src/pages/index.astro de demo), substitua-as pelos placeholders abaixo. Ajuste astro.config.mjs para saída estática (output 'static' é o default do Astro — apenas confirme que não há adaptador SSR). Garanta que o .gitignore inclua node_modules/, dist/, .astro/ (preservando o .gitignore existente — faça merge, não sobrescreva cegamente). Crie .nvmrc com "22". Crie as pastas src/components/, src/layouts/, src/styles/ com um .gitkeep cada (serão preenchidas em 01-02/01-03). Crie public/brand/ com .gitkeep.

    Crie as 4 páginas placeholder em src/pages/ — index.astro, solucoes.astro, suporte.astro, contato.astro. Cada uma: HTML mínimo e válido (sem layout ainda — BaseLayout vem em 01-03), com um <title>, um <h1> identificando a página, e uma lista de links de navegação simples para as outras 3 páginas (href="/", "/solucoes", "/suporte", "/contato") para que sejam comprovadamente navegáveis nesta fase. Texto em pt-BR. Deixe um comentário Astro indicando que conteúdo real vem na Fase 2 e o layout em 01-03. NÃO inclua estilos elaborados — apenas markup navegável.

    Rode `npm install` ao final para gerar package-lock.json e validar a instalação.
  </action>
  <verify>
    <automated>npm install && npx astro build && test -f dist/index.html && test -f dist/solucoes/index.html && test -f dist/suporte/index.html && test -f dist/contato/index.html && echo OK</automated>
  </verify>
  <done>`npm install` completa; `npx astro build` gera dist/ com as 4 páginas; estrutura src/{pages,components,layouts,styles} e public/brand/ existem.</done>
</task>

<task type="auto">
  <name>Task 2: Pipeline de assets (script assets:sync) + documentação de troca de logo</name>
  <files>scripts/sync-assets.mjs, package.json, public/brand/README.md</files>
  <action>
    Criar scripts/sync-assets.mjs — um script Node ESM (sem dependências externas; use node:fs/node:path) que copia os logos de "assets-input/logos/Novo Logo 2026/" para "public/brand/" renomeando para nomes limpos, kebab-case, sem espaços/bullets/acentos. Comportamento exigido:

    - Definir um MAPA explícito de variante de cor -> slug: "Branco" -> "branco"; "CLASSICA 01 • Sky Blue - Hex 81D2F1" -> "sky"; "CLASSICA 02 • Ocean Blue - 005A91" -> "ocean"; "Navy Dark - Hex 1B3A5C" -> "navy"; "Preto - Hex 000000" -> "preto"; "Soft Blue - Hex A3C5F1" -> "soft". (Os slugs ocean/navy/sky/soft casam com os data-theme do plano 01-02.)
    - Definir um MAPA de arquivo-origem -> nome-destino para os 5 formatos: "Favicon PNG 48 × 48 px.png" -> "favicon-48.png"; "Logo pequeno _ marca reduzida 512 × 512 px.png" -> "marca-512.png"; "Logo principal horizontal - 1024px.png" -> "horizontal-1024.png"; "Logo principal para alta resolução - 2048px.png" -> "horizontal-2048.png"; "Logo vertical_quadrado 1024 × 1024 px.png" -> "vertical-1024.png".
    - Para cada variante, copiar os 5 arquivos para "public/brand/<slug>/<nome-destino>". Copiar também "01 VETOR ORIGINAL.svg" para "public/brand/logo.svg".
    - Selecionar o FAVICON canônico do site: copiar o favicon-48 da variante "ocean" (primária default) para "public/brand/favicon.png" (consumido pelo BaseLayout em 01-03).
    - Selecionar logos canônicos default para o header (01-03 os usa): copiar horizontal-1024 da variante "ocean" para "public/brand/logo-horizontal.png" e horizontal-1024 da variante "branco" para "public/brand/logo-horizontal-branco.png" (para fundos escuros).
    - Ser idempotente (rodar duas vezes não duplica nem quebra). Logar cada cópia e um resumo final (N arquivos copiados). Se a pasta de origem não existir, falhar com mensagem clara apontando o caminho esperado. Pular silenciosamente os PNGs soltos de nome aleatório na raiz.

    Adicionar o script "assets:sync": "node scripts/sync-assets.mjs" em package.json. Verificar/garantir que public/brand/ está FORA do .gitignore (os assets sincronizados devem ser versionados para o site funcionar a partir do repo).

    Criar public/brand/README.md (pt-BR) documentando claramente (FND-07): (1) onde colocar/atualizar logos — "assets-input/logos/Novo Logo 2026/<variante de cor>/"; (2) os nomes de arquivo esperados em cada pasta de variante; (3) o comando para publicar no site — `npm run assets:sync`; (4) o que cada arquivo gerado em public/brand/ representa (favicon.png, logo-horizontal.png, logo-horizontal-branco.png, logo.svg e as subpastas por cor); (5) aviso de que os arquivos em public/brand/ são GERADOS — editar a origem em assets-input/, não o destino.
  </action>
  <verify>
    <automated>npm run assets:sync && test -f public/brand/favicon.png && test -f public/brand/logo-horizontal.png && test -f public/brand/logo-horizontal-branco.png && test -f public/brand/logo.svg && test -f public/brand/ocean/horizontal-1024.png && test -f public/brand/navy/favicon-48.png && test -f public/brand/README.md && echo SYNC_OK</automated>
  </verify>
  <done>`npm run assets:sync` copia os logos para public/brand/ com nomes limpos; favicon.png, logo-horizontal.png, logo-horizontal-branco.png e logo.svg existem; subpastas por cor (ocean/navy/sky/soft/branco/preto) populadas; README documenta o fluxo de troca.</done>
</task>

</tasks>

<verification>
1. `npm install` completa sem erros.
2. `npm run dev` sobe o servidor; visitar http://localhost:4321/, /solucoes, /suporte, /contato — todas abrem e os links de navegação funcionam.
3. `npm run build` gera dist/ com as 4 páginas (index, solucoes, suporte, contato).
4. `npm run assets:sync` popula public/brand/ com favicon.png, logos horizontais e subpastas por cor.
5. public/brand/README.md explica onde colocar logos e qual comando rodar.
</verification>

<success_criteria>
- Projeto Astro estático roda localmente com um comando (`npm run dev`) — FND-01.
- Pipeline `assets:sync` consome de assets-input/ e disponibiliza ao site em public/brand/ — FND-02.
- Troca de logo documentada de forma simples em public/brand/README.md — FND-07.
- 4 páginas placeholder navegáveis (esqueleto; conteúdo real e layout vêm depois).
</success_criteria>

<output>
Criar `.planning/phases/01-funda-o-base-visual/01-01-SUMMARY.md` ao concluir, registrando: versão do Astro instalada, estrutura de pastas final, mapa de slugs/arquivos do sync e os caminhos gerados em public/brand/ (para 01-03 consumir o logo e o favicon corretos).
</output>
