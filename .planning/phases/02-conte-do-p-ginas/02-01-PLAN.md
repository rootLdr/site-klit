---
phase: 02-conte-do-p-ginas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/solucoes.js
  - src/pages/index.astro
autonomous: true
requirements: [CNT-01, CNT-02, CNT-04, CNT-10]
must_haves:
  truths:
    - "A home abre com um hero de posicionamento fixo (sem slider), com headline, subheadline e dois CTAs (WhatsApp + Conheça as soluções)"
    - "A home mostra a seção de Segurança em destaque com os três produtos: Kofre, Sherlock e Eskudo"
    - "A seção de Segurança cita os parceiros Trend Micro e Cisco como prova"
    - "A home mostra a tese econômica com a frase 'investimento inicial nulo'"
    - "Existe um módulo de dados único que descreve as 9 soluções agrupadas nos 3 pilares, com os nomes de marca preservados"
    - "A home continua temável (4 temas) e responsiva, sem hex hardcoded nas novas seções"
  artifacts:
    - path: "src/data/solucoes.js"
      provides: "Módulo de dados compartilhado: 9 soluções (nome+slug+descritor+pilar) e os 3 pilares"
      contains: "Kofre"
      min_lines: 40
    - path: "src/pages/index.astro"
      provides: "Home parte 1: Hero, Segurança em destaque e tese econômica montados sobre BaseLayout"
      contains: "investimento inicial nulo"
      min_lines: 60
  key_links:
    - from: "src/pages/index.astro"
      to: "src/data/solucoes.js"
      via: "import do módulo de dados (pilar Segurança alimenta os 3 cards)"
      pattern: "import.*solucoes"
    - from: "src/pages/index.astro"
      to: "src/components/Button.astro"
      via: "CTAs do hero usam o primitivo Button"
      pattern: "Button"
---

<objective>
Criar o módulo de dados compartilhado das 9 soluções/3 pilares e montar a PRIMEIRA metade da home (Hero sem slider, Segurança em destaque e tese econômica) sobre o shell da Fase 1.

Purpose: O módulo de dados (`src/data/solucoes.js`) é a fonte única de verdade consumida pela home (02-02), pela seção de pilares e pela página /solucoes (02-03) — evita duplicar nomes/descritores e garante consistência das marcas. As três primeiras seções estabelecem o posicionamento fixo e elevam Segurança como diferencial (CNT-01, CNT-02, CNT-04).

Output: `src/data/solucoes.js` (dados) e `src/pages/index.astro` com Hero + Segurança em destaque + tese econômica (substituindo o esqueleto atual).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-conte-do-p-ginas/02-CONTEXT.md

<!-- Conteúdo factual a preservar (marcas, descritores, parceiros, posicionamento): -->
@docs/current-site-analysis.md

<interfaces>
<!-- Primitivos e tokens da Fase 1 — usar diretamente, sem explorar o codebase. -->
<!-- TODOS os componentes usam SOMENTE var(--token): zero hex hardcoded permitido. -->

Layout raiz — src/layouts/BaseLayout.astro:
  Props: title?: string, description?: string
  Renderiza <html data-theme="ocean"> + Header + <main><slot/></main> + Footer + WhatsAppButton.
  A home importa BaseLayout e injeta o conteúdo no <slot/>.

Primitivos (src/components/):
  Section.astro  — Props: tone?: "default"|"dark" (default "default"), id?: string. <section> com respiro vertical generoso; tone="dark" = fundo navy + texto claro.
  Container.astro — Props: size?: "sm"|"md"|"lg" (default "lg"), as?: tag. Largura máxima + padding lateral responsivo.
  Button.astro   — Props: variant?: "primary"|"secondary"|"ghost", href?: string, type?, ...rest. Renderiza <a> se houver href. variant="primary" segue var(--color-primary) (muda com o tema).
  Card.astro     — Props: title?: string, as?: tag. Superfície com surface/border/radius/shadow; título vira <h3>; conteúdo no slot.

Tokens semânticos disponíveis (src/styles/tokens.css) — usar SEMPRE estes, nunca hex:
  Cores: --color-primary, --color-primary-contrast, --color-accent, --color-text, --color-text-muted,
         --color-bg, --color-surface, --color-surface-alt, --color-border, --color-muted,
         --color-dark-bg, --color-dark-text, --color-dark-border, --color-dark-text-muted
  Espaço: --space-1..--space-9 (4px→96px)
  Raio: --radius-sm/md/lg/full ; Sombra: --shadow-sm/md/lg
  Tipografia: --font-sans, --font-size-xs..4xl, --line-tight/snug/base, --font-weight-regular/medium/semibold/bold
  Layout: --container-sm/md/lg, --container-pad ; Transições: --transition-fast/base
</interfaces>

<content_facts>
<!-- Copy FIXA desta fase (02-CONTEXT é a fonte de verdade). NÃO inventar números/claims. -->

Hero (CNT-01):
  Headline: "Infraestrutura de TI gerenciada e segura para sua empresa."
  Subheadline: "Do dado à rede à nuvem — com investimento inicial nulo e suporte de quem entende do seu negócio."
  CTA primário: "Falar no WhatsApp" → href "/contato" (o número real é Fase 3; aqui aponta para /contato OU âncora; ver action).
  CTA secundário: "Conheça as soluções" → href "/solucoes".

Segurança em destaque (CNT-02):
  Título: "Segurança de ponta a ponta"
  Subtítulo: proteção do dado → uso → rede.
  3 cards (marcas preservadas literalmente):
    Kofre — Segurança e gestão de documentos.
    Sherlock — Monitoramento de acesso à internet.
    Eskudo — Segurança de rede.
  Prova: citar os parceiros Trend Micro e Cisco.

Tese econômica (CNT-04):
  Destaque textual: "Investimento inicial nulo" (infraestrutura como serviço / Lokar): OPEX vs CAPEX, custo previsível, parceiro único.
  Mensagem de mobilidade (Konecte): "Trabalhe de qualquer lugar, a qualquer momento."
</content_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Módulo de dados das 9 soluções e 3 pilares</name>
  <files>src/data/solucoes.js</files>
  <action>
    Criar o módulo de dados compartilhado (fonte única de verdade) que será consumido pela home (pilares em 02-02), pela seção de Segurança e pela página /solucoes (02-03). Exportar duas estruturas:

    1) Um array/objeto `solucoes` com as 9 soluções. Para cada uma, campos: `nome` (marca preservada LITERALMENTE), `slug` (kebab-case, ex.: "kofre"), `descritor` (frase curta funcional dos content_facts/02-CONTEXT), `pilar` (chave do pilar) e `paragrafo` (1 parágrafo curto sugerido — a validar — usável na /solucoes; pode reaproveitar as reescritas de docs/current-site-analysis.md seção 12, sem inventar números/clientes).
       As 9 marcas e descritores (de 02-CONTEXT):
       - Kofre — Segurança e gestão de documentos (pilar: seguranca)
       - Sherlock — Monitoramento de acesso à internet (pilar: seguranca)
       - Eskudo — Segurança de rede (pilar: seguranca)
       - Lokar — Outsourcing / infraestrutura como serviço, investimento inicial nulo (pilar: infra-cloud)
       - Nuvem — Computação em nuvem (pilar: infra-cloud)
       - Virtualiza — Virtualização de ambientes (pilar: infra-cloud)
       - Konecte — Mobilidade e disponibilidade (pilar: infra-cloud)
       - Gesin — Gestão integrada e suporte técnico à infraestrutura (pilar: gestao-consultoria)
       - Konsulte — Consultoria em TI (pilar: gestao-consultoria)
    2) Um array `pilares` com os 3 pilares na ordem Segurança → Infraestrutura & Cloud → Gestão & Consultoria. Cada pilar com: `id` (ex.: "seguranca"), `titulo` ("Segurança", "Infraestrutura & Cloud", "Gestão & Consultoria") e `descricao` curta (sugerida). Exportar também um helper `solucoesPorPilar(pilarId)` OU manter os dados de forma que filtrar por pilar seja trivial (ex.: `solucoes.filter(s => s.pilar === id)`).

    Documentar no topo do arquivo, em comentário, que descritores/parágrafos são SUGERIDOS e a validar com o usuário, e que as marcas NÃO podem ser alteradas. Sem hex, sem dependências externas — só dados/JS puro.

    CONTRATO DE EXPORT (obrigatório — 02-02 e 02-03 importam assim): usar **named exports ESM**: `export const solucoes = [...]` e `export const pilares = [...]` (NÃO usar `export default`). Se criar o helper, também named: `export function solucoesPorPilar(id) {...}`. Os consumidores farão `import { solucoes, pilares } from "../data/solucoes.js"`.
  </action>
  <verify>
    <automated>node -e "const m=require('./src/data/solucoes.js'); /* fallback se for ESM: */" 2>/dev/null || node --input-type=module -e "import('./src/data/solucoes.js').then(m=>{const n=m.solucoes||m.default.solucoes; const names=n.map(s=>s.nome); ['Kofre','Sherlock','Eskudo','Lokar','Nuvem','Virtualiza','Konecte','Gesin','Konsulte'].forEach(x=>{if(!names.includes(x))throw new Error('falta '+x)}); if(n.length!==9)throw new Error('esperado 9, veio '+n.length); console.log('OK 9 soluções, marcas preservadas')})"</automated>
  </verify>
  <done>src/data/solucoes.js exporta as 9 soluções (marcas Kofre/Sherlock/Eskudo/Lokar/Nuvem/Virtualiza/Konecte/Gesin/Konsulte preservadas) e os 3 pilares; é importável por arquivos .astro.</done>
</task>

<task type="auto">
  <name>Task 2: Hero (sem slider) + Segurança em destaque + tese econômica na home</name>
  <files>src/pages/index.astro</files>
  <action>
    Substituir o esqueleto de index.astro pelas TRÊS primeiras seções da home, mantendo BaseLayout e importando os primitivos. Importar também `src/data/solucoes.js` e derivar os 3 cards de Segurança a partir do pilar "seguranca" do módulo (NÃO redigitar os nomes inline — consumir o módulo, garantindo consistência das marcas).

    Atualizar as props do BaseLayout: title "KLIT — Infraestrutura de TI gerenciada e segura", description curta de posicionamento (pt-BR). SEO completo é Fase 3.

    Seção 1 — Hero (CNT-01), id="hero": dentro de <Section> + <Container>. Headline (h1) e subheadline EXATAS dos content_facts. Dois CTAs com o primitivo Button: primário variant="primary" "Falar no WhatsApp" e secundário variant="secondary" "Conheça as soluções" href="/solucoes". Para o CTA de WhatsApp: o número real é Fase 3 — apontar href="/contato" e deixar comentário "// TODO(Fase 3): trocar para wa.me quando o número for fornecido". Sem slider, sem JS de rotação. Visual premium só com tokens (espaçamento generoso, tipografia da escala). Se houver imagem em assets-input/images/ aproveitar; senão, composição apenas com tokens (não baixar imagens de terceiros).

    Seção 2 — Segurança em destaque (CNT-02), id="seguranca": <Section> (pode usar tone padrão; o foco premium vem do conteúdo). Título "Segurança de ponta a ponta" e subtítulo sobre proteção do dado → uso → rede. Renderizar 3 <Card> (um por solução do pilar Segurança vindo do módulo), cada um com o nome de marca como título e o descritor no corpo. Abaixo dos cards, uma linha de prova citando os parceiros "Trend Micro" e "Cisco" (texto, sem logo de terceiro).

    Seção 3 — Tese econômica (CNT-04), id="investimento": <Section>. Destacar visualmente a frase "Investimento inicial nulo" (h2 ou destaque tipográfico) + parágrafo sobre OPEX vs CAPEX / custo previsível / parceiro único, e a mensagem de mobilidade "Trabalhe de qualquer lugar, a qualquer momento." (Konecte). Sem inventar números/percentuais.

    Estilos: bloco <style> escopado usando EXCLUSIVAMENTE var(--token). Zero hex. Garantir responsividade (grid de cards colapsa em coluna no mobile via media query com tokens). NÃO adicionar as seções de pilares/parceiros/institucional aqui — essas são do 02-02 e serão anexadas depois.
  </action>
  <verify>
    <automated>! grep -nE '#[0-9a-f]{3,6}' src/pages/index.astro src/data/solucoes.js && npx astro build 2>&1 | tail -5 && node -e "const fs=require('fs');const h=fs.readFileSync('dist/index.html','utf8');['investimento inicial nulo','Kofre','Sherlock','Eskudo','Trend Micro','Cisco','Conheça as soluções'].forEach(t=>{if(!new RegExp(t.replace(/[.*+?^]/g,'\\\\$&'),'i').test(h))throw new Error('home não contém: '+t)});if(/<div[^>]*slider|swiper|carousel/i.test(h))throw new Error('hero não pode ter slider');console.log('OK home parte 1: hero/segurança/tese presentes, sem slider')"
  </verify>
  <done>O build gera dist/index.html contendo o hero (headline+sub+2 CTAs, sem slider), a seção Segurança com Kofre/Sherlock/Eskudo + Trend Micro/Cisco, e a tese econômica com "investimento inicial nulo". Nenhum hex hardcoded nas novas seções (só var(--token)).</done>
</task>

</tasks>

<verification>
- `grep -niE "#[0-9a-f]{3,6}" src/pages/index.astro src/data/solucoes.js` retorna vazio (zero hex hardcoded).
- `npx astro preview` → `/` responde 200 e renderiza as 3 seções.
- Trocar de tema pelo ThemeSwitcher muda a cor dos CTAs/destaques (tokens funcionando) — verificável visualmente; automatizado cobre presença do conteúdo.
- As marcas Kofre/Sherlock/Eskudo na seção de Segurança vêm do módulo de dados (consistência), não digitadas inline.
</verification>

<success_criteria>
- src/data/solucoes.js exporta as 9 soluções com marcas preservadas e os 3 pilares.
- A home abre com hero de posicionamento fixo (sem slider) e Segurança em destaque (success criteria #1 do ROADMAP).
- CNT-01, CNT-02, CNT-04 e CNT-10 (conteúdo factual preservado) atendidos pela home parte 1.
- Tudo temável (4 temas) e responsivo, sem hex hardcoded.
</success_criteria>

<output>
Criar `.planning/phases/02-conte-do-p-ginas/02-01-SUMMARY.md` ao concluir.
</output>
