---
phase: 02-conte-do-p-ginas
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/pages/index.astro
autonomous: true
requirements: [CNT-03, CNT-05, CNT-06, CNT-10]
must_haves:
  truths:
    - "A home mostra as 9 soluções agrupadas nos 3 pilares (Segurança / Infraestrutura & Cloud / Gestão & Consultoria), com nomes de marca preservados"
    - "A home mostra uma faixa de parceiros listando VMware, Trend Micro, Microsoft, Cisco, SolarWinds, Google, Azure e AWS (texto/placeholder elegante, sem logos baixados de terceiros)"
    - "A home mostra o bloco institucional com Missão, Visão e Valores reescritos sem typos"
    - "A home termina com um CTA final (WhatsApp + Contato) e o telefone 51 3022-3066"
    - "As seções da parte 2 são acrescentadas APÓS as da parte 1, sem quebrar o hero/segurança/tese existentes"
  artifacts:
    - path: "src/pages/index.astro"
      provides: "Home completa: parte 1 (02-01) + pilares, parceiros, institucional e CTA final"
      contains: "Ser referência nacional"
      min_lines: 150
  key_links:
    - from: "src/pages/index.astro"
      to: "src/data/solucoes.js"
      via: "seção de pilares itera os 3 pilares e suas soluções a partir do módulo de dados"
      pattern: "pilares|solucoes"
---

<objective>
Anexar a SEGUNDA metade da home à index.astro: as 9 soluções em 3 pilares (consumindo o módulo de dados de 02-01), a faixa de parceiros (lista textual elegante, sem logos de terceiros), o bloco institucional (Missão/Visão/Valores reescritos sem typos) e o CTA final com contato rápido.

Purpose: Completa a narrativa da home — depois do hero, Segurança e tese econômica (02-01), o visitante vê o portfólio completo nos 3 pilares (CNT-03), a prova de parceiros (CNT-05), o institucional sem typos (CNT-06) e um fechamento com CTA. Reúne o conteúdo factual do site atual (CNT-10).

Output: `src/pages/index.astro` com as 4 seções finais acrescentadas após as de 02-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-conte-do-p-ginas/02-CONTEXT.md
@docs/current-site-analysis.md

<!-- Saída de 02-01: ler antes de editar para anexar (não recriar) as seções. -->
@.planning/phases/02-conte-do-p-ginas/02-01-SUMMARY.md

<interfaces>
<!-- Mesmos primitivos/tokens de 02-01. Usar SOMENTE var(--token): zero hex. -->
Section.astro  — Props: tone?: "default"|"dark", id?. tone="dark" = fundo navy + texto claro (bom para CTA final ou destaque institucional).
Container.astro — Props: size?: "sm"|"md"|"lg", as?.
Button.astro   — Props: variant?: "primary"|"secondary"|"ghost", href?, ...rest.
Card.astro     — Props: title?: string, as?. Título vira <h3>.

Módulo de dados (criado em 02-01) — src/data/solucoes.js:
  Exporta `solucoes` (9 itens: nome, slug, descritor, pilar, paragrafo) e `pilares` (3 itens: id, titulo, descricao).
  Ordem dos pilares: Segurança → Infraestrutura & Cloud → Gestão & Consultoria.
  Filtrar soluções de um pilar: solucoes.filter(s => s.pilar === pilar.id) (confirmar a API exata no arquivo gerado em 02-01).

Tokens: ver interfaces de 02-01 (cores semânticas, --space-*, --radius-*, --font-*, etc.). Nunca hex.
</interfaces>

<content_facts>
<!-- Copy FIXA (02-CONTEXT é a fonte de verdade). NÃO inventar números/clientes/claims. -->

Soluções em 3 pilares (CNT-03) — marcas preservadas, derivadas do módulo de dados:
  Pilar Segurança: Kofre, Sherlock, Eskudo.
  Pilar Infraestrutura & Cloud: Lokar, Nuvem, Virtualiza, Konecte.
  Pilar Gestão & Consultoria: Gesin, Konsulte.

Parceiros (CNT-05) — lista textual/placeholder elegante (NÃO baixar logos de terceiros):
  VMware, Trend Micro, Microsoft, Cisco, SolarWinds, Google, Azure, AWS.

Institucional (CNT-06) — reescrito, sem typos:
  Missão: "Conquistar e fidelizar clientes, garantindo eficácia aos usuários, mobilidade e redução de custos."
  Visão: "Ser referência nacional nos segmentos em que atua."
  Valores: Cliente satisfeito · Ética · Inovação · Sustentabilidade · Lucratividade.

CTA final + contato rápido:
  Reforço de CTA (WhatsApp + link Contato) e telefone 51 3022-3066.
</content_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pilares (9 soluções) + faixa de parceiros</name>
  <files>src/pages/index.astro</files>
  <action>
    LER o index.astro atual (saída de 02-01) e ANEXAR as seções APÓS a tese econômica — não reescrever as seções existentes (hero/segurança/tese permanecem). Importar `pilares` e `solucoes` do módulo `src/data/solucoes.js` no frontmatter (se 02-01 já importou parte, reaproveitar o import).

    Seção "Soluções em 3 pilares" (CNT-03), id="solucoes": <Section> + <Container>. Título da seção (ex.: "Soluções para toda a sua TI"). Iterar os 3 `pilares` na ordem do módulo (Segurança → Infra & Cloud → Gestão & Consultoria). Para cada pilar: subtítulo com `pilar.titulo` (+ descricao curta) e um grid de <Card> — um por solução daquele pilar (filtrando do módulo). Cada card: nome de marca como título + descritor no corpo. Adicionar link "Ver todas as soluções" → "/solucoes" (Button ghost/secondary). NÃO redigitar marcas inline; tudo vem do módulo (garante consistência e que as 9 apareçam).

    Seção "Parceiros" (CNT-05), id="parceiros": <Section> + <Container>. Título (ex.: "Tecnologias e parceiros"). Renderizar a lista textual dos 8 parceiros (VMware, Trend Micro, Microsoft, Cisco, SolarWinds, Google, Azure, AWS) como uma faixa/grid elegante de "chips"/nomes estilizados APENAS com tokens (sem imagens de terceiros). Deixar comentário "// TODO: logos oficiais a fornecer pelo usuário (não baixar logos de terceiros)". Definir os 8 nomes num array local no frontmatter para iterar.

    Estilos novos em <style> escopado, só var(--token). Grids responsivos colapsando em mobile. Zero hex.
  </action>
  <verify>
    <automated>! grep -nE '#[0-9a-f]{3,6}' src/pages/index.astro && npx astro build 2>&1 | tail -5 && node -e "const fs=require('fs');const h=fs.readFileSync('dist/index.html','utf8');['Lokar','Nuvem','Virtualiza','Konecte','Gesin','Konsulte','VMware','SolarWinds','Microsoft','Azure','AWS'].forEach(t=>{if(!new RegExp(t,'i').test(h))throw new Error('home não contém: '+t)});console.log('OK pilares (9 soluções) + 8 parceiros presentes')"</automated>
  </verify>
  <done>dist/index.html contém as 9 soluções agrupadas nos 3 pilares (todas as marcas presentes, vindas do módulo) e a faixa de parceiros com os 8 nomes (texto, sem logos de terceiros). Hero/segurança/tese de 02-01 intactos.</done>
</task>

<task type="auto">
  <name>Task 2: Institucional (Missão/Visão/Valores) + CTA final</name>
  <files>src/pages/index.astro</files>
  <action>
    ANEXAR as duas últimas seções após Parceiros (sem alterar as anteriores).

    Seção "Institucional" (CNT-06), id="sobre": <Section> + <Container>. Título (ex.: "A KLIT"). Apresentar Missão, Visão e Valores com os textos EXATOS dos content_facts (reescritos, SEM typos — atenção a "referência" com acento, "Técnico"/"Outsourcing" não aparecem aqui mas mantenha o padrão). Layout: Missão e Visão como dois blocos/cards; Valores como lista dos 5 itens (Cliente satisfeito · Ética · Inovação · Sustentabilidade · Lucratividade). Só tokens.

    Seção "CTA final + contato rápido": <Section tone="dark"> (destaque premium em navy) + <Container>. Headline de fechamento (ex.: "Vamos cuidar da TI da sua empresa?"), dois CTAs com Button: "Falar no WhatsApp" (href="/contato"; // TODO(Fase 3): wa.me) e "Ir para Contato" (href="/contato", variant secondary). Exibir o telefone 51 3022-3066 como link tel: (tel:+555130223066). Sem inventar e-mail/número de WhatsApp (Fase 3).

    Estilos só com var(--token); garantir contraste sobre fundo dark via tokens --color-dark-text. Responsivo. Zero hex.

    Revisão final de typos: garantir que NENHUM dos typos do site antigo (Outsoursing, Tecnico, referencia) apareça em index.astro.
  </action>
  <verify>
    <automated>! grep -nE '#[0-9a-f]{3,6}' src/pages/index.astro && npx astro build 2>&1 | tail -5 && node -e "const fs=require('fs');const h=fs.readFileSync('dist/index.html','utf8');['Ser referência nacional','Cliente satisfeito','Sustentabilidade','51 3022-3066'].forEach(t=>{if(!new RegExp(t,'i').test(h))throw new Error('home não contém: '+t)});['Outsoursing','Tecnico','referencia'].forEach(bad=>{if(new RegExp('\\\\b'+bad+'\\\\b').test(h))throw new Error('typo presente: '+bad)});console.log('OK institucional sem typos + CTA final com telefone')"</automated>
  </verify>
  <done>dist/index.html contém Missão/Visão/Valores reescritos (Visão com "referência"), os 5 valores e o CTA final com WhatsApp/Contato + telefone 51 3022-3066. Nenhum typo (Outsoursing/Tecnico/referencia) presente.</done>
</task>

</tasks>

<verification>
- `grep -niE "#[0-9a-f]{3,6}" src/pages/index.astro` retorna vazio (zero hex hardcoded — exceto se 02-01 já estiver limpo; confirmar).
- `npx astro preview` → `/` responde 200 e rola por TODAS as seções (hero → segurança → tese → pilares → parceiros → institucional → CTA final).
- As 9 soluções vêm do módulo de dados (consistência com 02-01 e /solucoes de 02-03).
- Troca de tema afeta cards/botões/CTA final (tokens).
</verification>

<success_criteria>
- As 9 soluções aparecem nos 3 pilares com marcas preservadas (ROADMAP success criteria #2; CNT-03).
- Parceiros e bloco institucional presentes, sem typos (success criteria #3; CNT-05, CNT-06).
- Conteúdo factual preservado (CNT-10).
- Home completa, temável e responsiva, sem hex hardcoded.
</success_criteria>

<output>
Criar `.planning/phases/02-conte-do-p-ginas/02-02-SUMMARY.md` ao concluir.
</output>
