---
phase: 02-conte-do-p-ginas
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/pages/solucoes.astro
  - src/pages/suporte.astro
  - src/pages/contato.astro
  - src/components/ContatoForm.astro
autonomous: true
requirements: [CNT-07, CNT-08, CNT-09, CNT-10]
must_haves:
  truths:
    - "A página /solucoes detalha as 9 soluções agrupadas nos 3 pilares, com nomes de marca preservados, consumindo o módulo de dados"
    - "A página /suporte mostra conteúdo real: telefone 51 3022-3066, WhatsApp, e-mail placeholder e um espaço reservado para acesso remoto marcado como a fornecer"
    - "A página /contato mostra um formulário (nome, e-mail, empresa, telefone, mensagem) com validação HTML, sem envio funcional (placeholder documentado para Fase 3)"
    - "A página /contato mostra o telefone 51 3022-3066, o endereço completo de Porto Alegre e o WhatsApp"
    - "As três páginas navegam pelo header e mantêm tema/responsividade (sem hex hardcoded)"
  artifacts:
    - path: "src/pages/solucoes.astro"
      provides: "Detalhe das 9 soluções nos 3 pilares, reusando o módulo de dados e Card"
      contains: "Konsulte"
      min_lines: 40
    - path: "src/pages/suporte.astro"
      provides: "Página de suporte real: canais + espaço de acesso remoto"
      contains: "51 3022-3066"
      min_lines: 40
    - path: "src/pages/contato.astro"
      provides: "Página de contato: formulário UI + dados reais (telefone, endereço, WhatsApp)"
      contains: "91330-001"
      min_lines: 40
    - path: "src/components/ContatoForm.astro"
      provides: "Formulário de contato com validação HTML e action placeholder (envio = Fase 3)"
      contains: "required"
      min_lines: 30
  key_links:
    - from: "src/pages/solucoes.astro"
      to: "src/data/solucoes.js"
      via: "itera os 3 pilares e suas soluções a partir do módulo de dados"
      pattern: "pilares|solucoes"
    - from: "src/pages/contato.astro"
      to: "src/components/ContatoForm.astro"
      via: "import e render do formulário"
      pattern: "ContatoForm"
---

<objective>
Criar as três páginas essenciais reusando o shell e o módulo de dados: /solucoes (detalhe das 9 nos 3 pilares), /suporte (conteúdo real com canais + espaço de acesso remoto) e /contato (formulário com validação HTML + dados reais de Porto Alegre).

Purpose: Substitui os esqueletos das páginas internas por conteúdo real navegável pelo header (CNT-07/08/09), preservando o conteúdo factual (CNT-10). O envio do formulário e o número real de WhatsApp ficam para a Fase 3 — aqui montamos só a UI/validação com action placeholder documentado.

Output: solucoes.astro, suporte.astro, contato.astro reescritos + componente ContatoForm.astro.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-conte-do-p-ginas/02-CONTEXT.md
@docs/current-site-analysis.md

<!-- Saída de 02-01: o módulo de dados é dependência desta plan (a página /solucoes o consome). -->
@.planning/phases/02-conte-do-p-ginas/02-01-SUMMARY.md

<interfaces>
<!-- Mesmos primitivos/tokens. Usar SOMENTE var(--token): zero hex. -->
BaseLayout.astro — Props: title?, description?. Header/Footer/WhatsApp vêm do layout.
Section.astro / Container.astro / Button.astro / Card.astro — ver 02-01.

Módulo de dados (criado em 02-01) — src/data/solucoes.js:
  `solucoes` (9 itens: nome, slug, descritor, pilar, paragrafo) e `pilares` (3: id, titulo, descricao).
  /solucoes deve usar o campo `paragrafo` (1 parágrafo curto por solução) além do descritor.
  Filtrar por pilar: solucoes.filter(s => s.pilar === pilar.id) (confirmar API exata no arquivo de 02-01).

Tokens: ver 02-01. Nunca hex.
</interfaces>

<content_facts>
<!-- Copy/dados FIXOS (02-CONTEXT + análise). NÃO inventar números/clientes/claims. -->

/solucoes (CNT-07): as 9 soluções nos 3 pilares (mesma taxonomia da home), cada uma com nome + descritor + 1 parágrafo curto (sugerido, a validar). Marcas preservadas.

/suporte (CNT-08):
  Canais: telefone 51 3022-3066 (tel:+555130223066), WhatsApp, e-mail placeholder (ex.: contato@klit.com.br — A CONFIRMAR).
  Texto de atendimento/SLA: genérico e sóbrio, SEM inventar números (sem "atendemos em X horas" específico).
  Espaço reservado p/ acesso remoto (TeamViewer/AnyDesk): bloco "Acesso remoto — em breve / a fornecer". NÃO embutir libs/SDKs de terceiros.

/contato (CNT-09):
  Formulário: campos nome, e-mail, empresa, telefone, mensagem. Validação HTML (required, type=email, type=tel). Botão "Enviar mensagem".
  Envio NÃO funcional nesta fase: action placeholder documentado (// TODO(Fase 3, ENT-02): integrar serviço externo p.ex. Formspree).
  Dados reais: telefone 51 3022-3066 (tel:), endereço "Av. Doutor Nilo Peçanha 3238, 2º andar, Chácara das Pedras, Porto Alegre/RS, CEP 91330-001", botão/linha de WhatsApp. Mapa é opcional/Fase 3.
</content_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Página /solucoes — detalhe das 9 nos 3 pilares</name>
  <files>src/pages/solucoes.astro</files>
  <action>
    Substituir o esqueleto de solucoes.astro. Importar BaseLayout, Section, Container, Card e `pilares`/`solucoes` de `src/data/solucoes.js`. Atualizar title "KLIT — Soluções" e description de posicionamento (pt-BR).

    Hero curto da página (Section + Container): h1 "Soluções" + parágrafo de introdução (posicionamento: do dado à rede à nuvem). Depois, iterar os 3 `pilares` na ordem do módulo. Para cada pilar: <Section> com h2 = pilar.titulo + descrição curta, e um grid de <Card> — um por solução do pilar (filtrando do módulo). Cada card: nome de marca (título) + descritor + o `paragrafo` curto (a validar). Garantir que as 9 marcas apareçam (Kofre, Sherlock, Eskudo, Lokar, Nuvem, Virtualiza, Konecte, Gesin, Konsulte) — todas vindas do módulo, nada digitado inline.

    Ao final, um CTA simples (Button "Falar no WhatsApp" → /contato; // TODO(Fase 3)). Estilos só com var(--token); grids responsivos; zero hex.
  </action>
  <verify>
    <automated>npx astro build 2>&1 | tail -5 && node -e "const fs=require('fs');const h=fs.readFileSync('dist/solucoes/index.html','utf8');['Kofre','Sherlock','Eskudo','Lokar','Nuvem','Virtualiza','Konecte','Gesin','Konsulte'].forEach(t=>{if(!new RegExp(t,'i').test(h))throw new Error('/solucoes não contém: '+t)});console.log('OK /solucoes com as 9 soluções nos 3 pilares')"</automated>
  </verify>
  <done>dist/solucoes/index.html lista as 9 soluções agrupadas nos 3 pilares, com marcas preservadas (vindas do módulo) e parágrafos. Sem hex hardcoded.</done>
</task>

<task type="auto">
  <name>Task 2: Página /suporte — conteúdo real (canais + acesso remoto)</name>
  <files>src/pages/suporte.astro</files>
  <action>
    Substituir o esqueleto de suporte.astro. Importar BaseLayout, Section, Container, Card, Button. Atualizar title "KLIT — Suporte" e description (pt-BR).

    h1 "Suporte" + parágrafo introdutório sóbrio sobre suporte da KLIT (ponto único de contato / Gesin), SEM inventar SLA numérico.

    Seção "Canais de atendimento": cards/blocos com:
      - Telefone: 51 3022-3066 como link tel:+555130223066.
      - WhatsApp: link/botão (href="/contato" ou comentário // TODO(Fase 3): wa.me — número real é Fase 3).
      - E-mail: placeholder contato@klit.com.br com comentário "// A CONFIRMAR com o usuário".
    Seção "Acesso remoto": bloco reservado com título "Acesso remoto" e texto "Em breve — ferramentas de acesso remoto (TeamViewer/AnyDesk) a fornecer." NÃO embutir libs/SDKs/scripts de terceiros; apenas placeholder textual marcado.
    Seção "Horário de atendimento": texto genérico sóbrio (sem inventar horário específico se não houver dado — pode ser "horário comercial" e comentário // A CONFIRMAR).

    Estilos só com var(--token); responsivo; zero hex.
  </action>
  <verify>
    <automated>npx astro build 2>&1 | tail -5 && node -e "const fs=require('fs');const h=fs.readFileSync('dist/suporte/index.html','utf8');['51 3022-3066','Acesso remoto','WhatsApp'].forEach(t=>{if(!new RegExp(t,'i').test(h))throw new Error('/suporte não contém: '+t)});if(/teamviewer\\.com|anydesk\\.com|<script[^>]+src=[\"']https?:\\/\\/(?!.*astro)/i.test(h)){console.warn('checar: possível lib de terceiro embutida')};console.log('OK /suporte com canais + acesso remoto placeholder')"</automated>
  </verify>
  <done>dist/suporte/index.html mostra telefone 51 3022-3066, WhatsApp, e-mail placeholder e o bloco "Acesso remoto" marcado como a fornecer. Nenhuma lib de acesso remoto embutida. Sem hex hardcoded.</done>
</task>

<task type="auto">
  <name>Task 3: Formulário de contato + página /contato com dados reais</name>
  <files>src/components/ContatoForm.astro, src/pages/contato.astro</files>
  <action>
    Criar src/components/ContatoForm.astro: um <form> com os campos nome, e-mail, empresa, telefone, mensagem e validação HTML nativa:
      - nome: <input type="text" required>
      - e-mail: <input type="email" required>
      - empresa: <input type="text"> (opcional)
      - telefone: <input type="tel"> (opcional; pattern leve opcional)
      - mensagem: <textarea required>
      - botão submit: "Enviar mensagem" (usar Button type="submit" ou <button> estilizado por tokens).
    Action placeholder DOCUMENTADO: no <form>, deixar action="#" (ou method sem endpoint) e um comentário proeminente "// TODO(Fase 3, ENT-02): integrar serviço externo de envio (ex.: Formspree). Envio NÃO funcional nesta fase." Adicionar microcopy curto (ex.: "Respondemos pelo WhatsApp ou e-mail.") sem prometer prazo numérico. Cada campo com <label> associado (acessibilidade). Estilos escopados só com var(--token); zero hex.

    Reescrever src/pages/contato.astro: importar BaseLayout, Section, Container, Button e ContatoForm. title "KLIT — Contato", description (pt-BR). Layout em duas colunas (responsivo, colapsa em mobile):
      - Coluna formulário: <ContatoForm />.
      - Coluna dados de contato: telefone 51 3022-3066 (link tel:+555130223066); endereço completo "Av. Doutor Nilo Peçanha 3238, 2º andar, Chácara das Pedras, Porto Alegre/RS, CEP 91330-001"; linha/botão WhatsApp (href="/contato" ou // TODO(Fase 3): wa.me — número real é Fase 3). Mapa embed é opcional e pode ficar para Fase 3 (não implementar agora).
    Estilos só com var(--token); responsivo; zero hex.
  </action>
  <verify>
    <automated>npx astro build 2>&1 | tail -5 && node -e "const fs=require('fs');const h=fs.readFileSync('dist/contato/index.html','utf8');['91330-001','51 3022-3066','Nilo Peçanha','WhatsApp'].forEach(t=>{if(!new RegExp(t,'i').test(h))throw new Error('/contato não contém: '+t)});['name=','required'].forEach(t=>{if(!h.includes(t))throw new Error('form sem: '+t)});if(/type=[\"']email[\"']/i.test(h)===false)throw new Error('form sem validação de e-mail');console.log('OK /contato: form com validação + telefone/endereço/WhatsApp')"</automated>
  </verify>
  <done>dist/contato/index.html contém o formulário (nome/e-mail/empresa/telefone/mensagem com required e type=email) sem envio funcional (action placeholder documentado), além do telefone 51 3022-3066, endereço completo de Porto Alegre (CEP 91330-001) e WhatsApp. Sem hex hardcoded.</done>
</task>

</tasks>

<verification>
- `grep -niE "#[0-9a-f]{3,6}" src/pages/solucoes.astro src/pages/suporte.astro src/pages/contato.astro src/components/ContatoForm.astro` retorna vazio (zero hex).
- `npx astro preview` → `/solucoes`, `/suporte`, `/contato` respondem 200 e navegam pelo header.
- /solucoes consome o mesmo módulo de dados de 02-01 (taxonomia idêntica à home).
- Form de /contato NÃO envia (action placeholder); envio real é Fase 3 (ENT-02). Número de WhatsApp real também é Fase 3.
- Nenhuma lib de acesso remoto embutida em /suporte (apenas placeholder textual).
</verification>

<success_criteria>
- Páginas Soluções, Suporte e Contato existem e navegam pelo header (ROADMAP success criteria #4; CNT-07/08/09).
- Telefone e endereço reais (Porto Alegre) aparecem no Contato (success criteria #5; CNT-09).
- Conteúdo factual preservado (CNT-10).
- Tudo temável (4 temas) e responsivo, sem hex hardcoded; envio de form e WhatsApp adiados p/ Fase 3.
</success_criteria>

<output>
Criar `.planning/phases/02-conte-do-p-ginas/02-03-SUMMARY.md` ao concluir.
</output>
