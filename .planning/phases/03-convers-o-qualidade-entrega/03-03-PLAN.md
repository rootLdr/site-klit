---
phase: 03-convers-o-qualidade-entrega
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - README.md
autonomous: true
requirements: [ENT-07, ENT-08]
must_haves:
  truths:
    - "Existe um README.md na raiz, em pt-BR, explicando como rodar (install/dev/build/preview), a estrutura de pastas e onde colocar assets"
    - "O README documenta como trocar logo, textos, solucoes (src/data/solucoes.js), contato/WhatsApp/Formspree (src/data/site.js) e temas/cores (src/styles/tokens.css)"
    - "O README lista as pendencias/placeholders do usuario (WhatsApp a confirmar, formspreeId a colar, e-mail e horario a confirmar)"
    - "npm run build gera dist/ com as 4 paginas e npm run preview serve localmente"
  artifacts:
    - path: "README.md"
      provides: "Guia de manutencao do site (rodar, estrutura, assets, troca de conteudo, pendencias)"
      contains: "npm run dev"
      min_lines: 60
  key_links:
    - from: "README.md"
      to: "src/data/site.js"
      via: "secao de como trocar contato/WhatsApp/Formspree"
      pattern: "site\\.js"
    - from: "README.md"
      to: "src/styles/tokens.css"
      via: "secao de como trocar temas/cores"
      pattern: "tokens\\.css"
---

<objective>
ENTREGAR o projeto: README.md de manutencao na raiz e confirmacao do build de producao + preview local.

Purpose: Fecha ENT-07 (README de manutencao) e ENT-08 (build de producao geravel/servivel). O usuario precisa saber rodar, entender a estrutura, onde colocar assets e como trocar logo/textos/solucoes/contato/temas - alem de ter ciencia dos placeholders a confirmar (WhatsApp, Formspree, e-mail, horario).

Output: README.md completo em pt-BR e a validacao de que `npm run build` gera dist/ (4 paginas) e `npm run preview` serve localmente.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-convers-o-qualidade-entrega/03-CONTEXT.md

# Fonte da verdade para a secao de scripts e estrutura:
@package.json

<interfaces>
<!-- Mapa real do projeto para o README documentar (verificado em disco). -->

Scripts (package.json):
- npm install            # instala dependencias
- npm run dev            # astro dev (servidor local)
- npm run build          # astro build (gera dist/)
- npm run preview        # astro preview (serve dist/ localmente)
- npm run assets:sync    # node scripts/sync-assets.mjs (consome assets-input/ -> public/brand/)

Onde trocar cada coisa (resultado das Fases 1-3):
- Logo / imagens de marca: assets-input/ (+ npm run assets:sync) -> public/brand/. Logo do Header em src/components/Header.astro; logo branco do Footer em src/components/Footer.astro.
- Textos das paginas: src/pages/{index,solucoes,suporte,contato}.astro
- Solucoes (9 marcas / 3 pilares): src/data/solucoes.js (named ESM exports)
- Contato / WhatsApp / Formspree: src/data/site.js (named ESM exports: telefone, whatsapp, whatsappUrl, email, endereco, formspreeId)
- Temas / cores / tipografia / espacamento: src/styles/tokens.css (4 temas via [data-theme]; seletor de tema = ThemeSwitcher; persiste em localStorage klit-theme)

Pendencias/placeholders a confirmar com o usuario (do CONTEXT.md):
- WhatsApp 555130223066 e o telefone fixo - confirmar se tem WhatsApp; se houver celular comercial, trocar `whatsapp` em src/data/site.js.
- Criar o form no Formspree e colar o ID em `formspreeId` (src/data/site.js); enquanto for SEU_FORM_ID o envio nao funciona.
- E-mail oficial e horario de atendimento (placeholders no site).
- Logos oficiais de parceiros e imagens de hero (opcional).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escrever README.md de manutencao na raiz</name>
  <files>README.md</files>
  <action>
    Criar `README.md` na raiz em pt-BR (ENT-07) com as secoes abaixo, usando os fatos do bloco interfaces (verificados em disco - nao inventar scripts/caminhos):

    1. Titulo + 1 paragrafo: o que e o projeto (site institucional estatico da KLIT em Astro; MSP de TI com Seguranca em destaque; sem backend).
    2. Requisitos: Node.js (versao compativel com Astro 6) e npm.
    3. Como rodar localmente: bloco de comandos com `npm install`, `npm run dev` (dev server), e nota de que abre no navegador.
    4. Build de producao e preview: `npm run build` (gera dist/) e `npm run preview` (serve dist/ localmente). Mencionar que a saida e estatica (output: static).
    5. Estrutura de pastas: arvore resumida (src/pages, src/components, src/layouts, src/data, src/styles, public/brand, assets-input, scripts).
    6. Onde colocar assets: assets-input/ + `npm run assets:sync` -> public/brand/ (pipeline idempotente). Onde estao os logos canonicos.
    7. Como trocar (subsecoes claras):
       - Logo: assets-input/ + assets:sync; trocar src no Header (logo colorido) e Footer (logo branco).
       - Textos: src/pages/*.astro.
       - Solucoes (9 marcas / 3 pilares): src/data/solucoes.js.
       - Contato / WhatsApp / Formspree: src/data/site.js (citar telefone, whatsapp, whatsappUrl, email, endereco, formspreeId).
       - Temas / cores: src/styles/tokens.css (4 temas via [data-theme]; ThemeSwitcher persiste em localStorage klit-theme).
    8. Pendencias / a confirmar: listar os 4 placeholders (WhatsApp a confirmar, formspreeId a colar para o form funcionar, e-mail oficial, horario) - exatamente como em interfaces. Explicar que ate colar o formspreeId o formulario mostrara erro (comportamento esperado).
    9. (Opcional, 1 linha) Como publicar: hospedar a pasta dist/ em qualquer host estatico; ajustar `site` em astro.config.mjs para o dominio final.

    NAO inventar comandos/paths inexistentes. NAO documentar features fora do escopo (sem CMS/backend). Texto objetivo (o usuario prefere conciso).
  </action>
  <verify>
    <automated>test -f README.md && for s in "npm run dev" "npm run build" "npm run preview" "assets:sync" "src/data/site.js" "src/data/solucoes.js" "src/styles/tokens.css" "formspreeId"; do grep -q "$s" README.md || { echo "README sem: $s"; exit 1; }; done && lines=$(grep -vc '^[[:space:]]*$' README.md) && test "$lines" -ge 50 && echo "README OK ($lines linhas nao-vazias)"</automated>
  </verify>
  <done>README.md na raiz cobre: rodar (install/dev/build/preview), estrutura, onde colocar assets (assets-input + assets:sync), como trocar logo/textos/solucoes/contato/WhatsApp/Formspree/temas, e as pendencias/placeholders. Sem comandos inventados.</done>
</task>

<task type="auto">
  <name>Task 2: Confirmar build de producao e preview local (ENT-08)</name>
  <files>README.md</files>
  <action>
    Validar a entrega buildavel (ENT-08), sem alterar codigo do site (apenas confirmacao; se o build falhar, e um bloqueio a reportar, nao um conserto de feature aqui). Rodar `npm run build` e confirmar que dist/ contem as 4 paginas (index, solucoes, contato, suporte) + dist/robots.txt (de 03-02). Subir `npm run preview` em background apenas para smoke test das rotas (/ retorna 200) e encerrar.

    Se o build passar e o preview responder, registrar no README (na secao de build) uma nota curta confirmando que o build de producao foi validado. Se algo falhar, NAO mascarar: reportar como bloqueio no SUMMARY (o build e a condicao de aceite de ENT-08).

    Este task nao toca codigo de componentes/paginas/dados - so executa comandos e, no maximo, ajusta a nota de validacao no README.
  </action>
  <verify>
    <automated>npx astro build && test -f dist/index.html && test -f dist/solucoes/index.html && test -f dist/contato/index.html && test -f dist/suporte/index.html && test -f dist/robots.txt && echo "build de producao OK (4 paginas + robots.txt)"</automated>
  </verify>
  <done>npm run build gera dist/ com as 4 paginas e robots.txt; preview serve / com 200; nota de validacao registrada no README. Sem alteracoes em codigo de feature.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhuma nova) | Plano de documentacao + validacao de build; sem novas entradas externas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Information Disclosure | README | accept | README contem apenas instrucoes publicas; nenhum segredo/credencial (formspreeId e identificador publico, nao secreto) |
| T-03-SC | Tampering | npm installs | mitigate | Nenhum pacote novo; apenas `npm run build`/`preview` com deps ja auditadas nas fases anteriores |
</threat_model>

<verification>
- README.md existe na raiz com todas as secoes obrigatorias (grep dos termos-chave).
- `npx astro build` passa e dist/ tem as 4 paginas + robots.txt.
- `npm run preview` serve / com 200 (smoke test).
</verification>

<success_criteria>
- ENT-07: README de manutencao completo (rodar, estrutura, assets, trocar conteudo, pendencias).
- ENT-08: build de producao gerado e servivel localmente (4 paginas + robots.txt).
</success_criteria>

<output>
Create `.planning/phases/03-convers-o-qualidade-entrega/03-03-SUMMARY.md` when done
</output>
