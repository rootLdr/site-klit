---
phase: 03-convers-o-qualidade-entrega
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/layouts/BaseLayout.astro
  - src/components/Header.astro
  - public/robots.txt
  - src/pages/index.astro
  - src/pages/contato.astro
  - src/pages/solucoes.astro
  - src/pages/suporte.astro
autonomous: true
requirements: [ENT-04, ENT-05, ENT-06]
must_haves:
  truths:
    - "Cada uma das 4 paginas tem title e meta description reais e distintos no HTML gerado"
    - "O BaseLayout emite Open Graph (og:title/description/type/url/image), twitter:card e link canonical por pagina"
    - "Existe public/robots.txt servido em /robots.txt permitindo indexacao"
    - "Ha um link skip-to-content no inicio do body que leva ao <main id=conteudo>; cada pagina tem exatamente um <h1>; os logos tem alt; o foco e visivel"
    - "O logo do Header tem width/height e estrategia de carregamento que evita layout shift"
  artifacts:
    - path: "src/layouts/BaseLayout.astro"
      provides: "SEO real (OG/twitter/canonical via props), skip-to-content, slot de SEO por pagina"
      contains: "og:title"
    - path: "public/robots.txt"
      provides: "robots.txt allow-all com referencia de sitemap/host"
      contains: "User-agent"
    - path: "src/components/Header.astro"
      provides: "Logo otimizado (width/height + loading/decoding) sem layout shift"
      contains: "width"
  key_links:
    - from: "src/pages/index.astro"
      to: "BaseLayout props SEO"
      via: "title/description/ogImage passados por pagina"
      pattern: "description="
    - from: "BaseLayout"
      to: "canonical/og:url"
      via: "Astro.url + site"
      pattern: "canonical"
    - from: "body"
      to: "main#conteudo"
      via: "skip-to-content href=#conteudo"
      pattern: "#conteudo"
---

<objective>
Elevar a QUALIDADE do site: SEO basico real (title/description por pagina, Open Graph, twitter:card, canonical, robots.txt), acessibilidade basica (um h1 por pagina, alt nos logos, foco visivel, skip-to-content, teclado, aria-live ja entregue em 03-01) e performance do logo do Header (sem layout shift). Revisao final de responsividade.

Purpose: Fecha ENT-04 (SEO), ENT-05 (a11y) e ENT-06 (performance). Hoje o BaseLayout so tem title/description placeholder e nao ha OG/canonical/robots; falta skip-to-content; o logo do Header nao tem height/loading otimizados.

Output: BaseLayout com bloco de SEO completo (props por pagina) + skip-to-content; robots.txt; titles/descriptions reais nas 4 paginas; logo do Header otimizado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-convers-o-qualidade-entrega/03-CONTEXT.md

# Arquivos a evoluir (ja implementados nas Fases 1-2)
@src/layouts/BaseLayout.astro
@src/components/Header.astro

<interfaces>
<!-- BaseLayout ja expoe props title/description. Estender o contrato de props. -->

Contrato de Props alvo do BaseLayout (estender o atual):
```ts
interface Props {
  title?: string;        // ja existe
  description?: string;  // ja existe
  ogImage?: string;      // novo - default "/brand/ocean/horizontal-2048.png" (asset existente)
  ogType?: string;       // novo - default "website"
}
```

Canonical/og:url: derivar de `new URL(Astro.url.pathname, Astro.site)` (astro.config tem site: 'https://klit.com.br').

Assets de OG disponiveis (ja versionados em public/brand/):
- /brand/ocean/horizontal-2048.png  (logo colorido grande - bom para og:image)
- /brand/favicon.png                (favicon ja referenciado)

<main id="conteudo"> ja existe no BaseLayout - o skip link deve apontar para #conteudo.
.visually-hidden ja existe em src/styles/global.css (reusar para o texto do skip link / padrao de foco).
</interfaces>

# NAO tocar src/data/site.js nem ContatoForm/Footer/WhatsAppButton (sao do 03-01, ja concluido neste wave anterior).
</context>

<tasks>

<task type="auto">
  <name>Task 1: SEO completo no BaseLayout (OG/twitter/canonical) + skip-to-content + robots.txt</name>
  <files>src/layouts/BaseLayout.astro, public/robots.txt</files>
  <action>
    Estender o BaseLayout (ENT-04, ENT-05) preservando TUDO que ja funciona (anti-flash canonico is:inline, favicon, fonte/tokens via global.css, Header/slot/Footer/WhatsAppButton, lang=pt-BR, data-theme=ocean).

    1. Props: adicionar `ogImage` (default "/brand/ocean/horizontal-2048.png") e `ogType` (default "website") ao interface Props e ao destructuring, mantendo title/description.
    2. Canonical/og:url: no frontmatter, computar `const canonical = new URL(Astro.url.pathname, Astro.site).href;` e `const ogImageAbs = new URL(ogImage, Astro.site).href;` (Astro.site vem de astro.config = https://klit.com.br).
    3. No <head>, apos a meta description, adicionar:
       - `<link rel="canonical" href={canonical} />`
       - Open Graph: og:type (=ogType), og:title (=title), og:description (=description), og:url (=canonical), og:image (=ogImageAbs), og:site_name ("KLIT"), og:locale ("pt_BR").
       - Twitter: `<meta name="twitter:card" content="summary_large_image" />`, twitter:title, twitter:description, twitter:image (=ogImageAbs).
       Usar a sintaxe `<meta property="og:..." content={...} />` para OG e `<meta name="twitter:..." ... />` para Twitter.
    4. Skip-to-content (ENT-05): logo no inicio do <body>, ANTES do <Header />, adicionar `<a href="#conteudo" class="skip-link">Pular para o conteudo</a>`. Adicionar um <style> escopado para `.skip-link`: visualmente escondido fora da tela por padrao e VISIVEL ao receber foco (:focus) - posicionando no topo, com fundo --color-primary e texto --color-primary-contrast, padding via --space-*, z-index alto. ZERO hex (so var(--token)). O <main id="conteudo"> ja existe; nao duplicar.

    Criar `public/robots.txt` (ENT-04): allow-all simples. Conteudo:
    `User-agent: *` / `Allow: /` / linha em branco / `Sitemap: https://klit.com.br/sitemap.xml` (referencia; mesmo sem sitemap gerado e inofensivo e documenta a intencao) / `Host: klit.com.br`. NAO instalar @astrojs/sitemap (manter simples - decisao do CONTEXT.md: pular sitemap se nao trivial).
  </action>
  <verify>
    <automated>grep -q "og:title" src/layouts/BaseLayout.astro && grep -q "og:image" src/layouts/BaseLayout.astro && grep -q "twitter:card" src/layouts/BaseLayout.astro && grep -q "rel=\"canonical\"" src/layouts/BaseLayout.astro && grep -q "skip-link" src/layouts/BaseLayout.astro && grep -q "#conteudo" src/layouts/BaseLayout.astro && test -f public/robots.txt && grep -qi "User-agent" public/robots.txt && grep -q "is:inline" src/layouts/BaseLayout.astro && ! grep -niE "#[0-9a-fA-F]{3,6}" src/layouts/BaseLayout.astro && echo "BaseLayout SEO + skip + robots OK"</automated>
  </verify>
  <done>BaseLayout emite canonical + OG (title/description/type/url/image/site_name/locale) + twitter:card por pagina; tem skip-link para #conteudo (escondido, visivel no foco, so tokens); anti-flash is:inline preservado; public/robots.txt existe com User-agent/Allow. Zero hex.</done>
</task>

<task type="auto">
  <name>Task 2: Titles/descriptions reais por pagina + um h1/pagina + alt + logo do Header otimizado</name>
  <files>src/pages/index.astro, src/pages/contato.astro, src/pages/solucoes.astro, src/pages/suporte.astro, src/components/Header.astro</files>
  <action>
    SEO por pagina (ENT-04): garantir title e description reais e DISTINTOS em cada pagina, passados ao BaseLayout. Os 4 ja passam title/description hoje - revisar para que sejam descritivos e unicos (≈50-60 chars no title, ≈140-160 no description), sem inventar fatos (sem numeros de clientes/SLA). Sugestoes (ajustar se redundante):
    - index: title "KLIT - Infraestrutura de TI gerenciada e segura" (manter), description de posicionamento (seguranca em destaque).
    - solucoes: title "Solucoes - KLIT", description citando os 3 pilares.
    - suporte: title "Suporte - KLIT", description sobre ponto unico de contato.
    - contato: title "Contato - KLIT", description com Porto Alegre/telefone/WhatsApp.
    Passar tambem `ogImage` so se quiser variar (opcional; default ja serve). NAO alterar o conteudo factual do corpo das paginas.

    Acessibilidade (ENT-05):
    - Confirmar UM unico <h1> por pagina (ja ha um por pagina: Hero/Solucoes/Suporte/Contato). Se alguma pagina tiver mais de um <h1>, rebaixar os extras para <h2>. NAO criar novos h1.
    - Garantir alt significativo nos logos: Header (alt="KLIT") e Footer ja tem alt; confirmar/manter. (Footer e do 03-01 ja concluido - NAO editar Footer aqui, so verificar no build.)

    Performance do logo do Header (ENT-06): no `<img>` do logo em Header.astro, garantir `width` e `height` explicitos (ja ha width=160 height=44) para reservar espaco e evitar layout shift; adicionar `loading="eager"` (esta acima da dobra, no header sticky) e manter `decoding="async"`. Opcionalmente, usar astro:assets <Image> so se trivial; caso contrario, manter <img> com width/height/loading/decoding (decisao do CONTEXT.md aceita ambas). NAO mudar a logica de troca de logo claro/escuro nem os estilos. ZERO hex.

    Revisao final de responsividade (ENT-05/qualidade): conferir no build que nao ha overflow-x (a guarda global ja existe em global.css) - sem mudancas de layout salvo se algo quebrar.
  </action>
  <verify>
    <automated>for f in index contato solucoes suporte; do c=$(grep -c "<h1" "src/pages/$f.astro"); test "$c" -eq 1 || { echo "h1 count=$c em $f (esperado 1)"; exit 1; }; grep -q "description=" "src/pages/$f.astro" || { echo "sem description em $f"; exit 1; }; done; grep -q "loading=\"eager\"" src/components/Header.astro && grep -q 'alt="KLIT"' src/components/Header.astro && grep -q 'width' src/components/Header.astro && ! grep -niE "#[0-9a-fA-F]{3,6}" src/components/Header.astro && npx astro build && for p in index contato/index solucoes/index suporte/index; do grep -q "og:title" "dist/$p.html" || { echo "sem og:title em $p"; exit 1; }; done && grep -q "robots" <(ls dist) ; ls dist/robots.txt && echo "SEO/a11y/perf OK"</automated>
  </verify>
  <done>Cada pagina tem exatamente um h1 e title/description distintos; o build mostra og:title nas 4 paginas e dist/robots.txt existe; logo do Header com width/height + loading=eager + decoding=async + alt=KLIT; zero hex.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crawlers -> robots.txt/meta | Bots de busca leem diretivas publicas (sem segredos expostos) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Information Disclosure | meta OG / canonical | accept | Apenas dados publicos institucionais; canonical aponta para o host de producao (klit.com.br) - sem vazamento |
| T-03-05 | Tampering | robots.txt | accept | Conteudo estatico allow-all sem regras sensiveis; servido como asset publico |
| T-03-SC | Tampering | npm installs | mitigate | NENHUM pacote novo (sem @astrojs/sitemap nesta fase, por decisao do CONTEXT.md); zero superficie de supply-chain |
</threat_model>

<verification>
- `npx astro build` passa; dist/{index,contato/index,solucoes/index,suporte/index}.html contem og:title; dist/robots.txt existe.
- Cada pagina tem exatamente um <h1>; logo do Header com width/height/loading/decoding/alt.
- BaseLayout preserva o anti-flash is:inline e o favicon; skip-link presente apontando para #conteudo.
- Zero hex nos arquivos tocados.
</verification>

<success_criteria>
- ENT-04: title/description reais por pagina + Open Graph + twitter:card + canonical + favicon + robots.txt.
- ENT-05: um h1/pagina, alt nos logos, skip-to-content, foco visivel, navegacao por teclado (menu/switcher/form ja funcionais), aria-live (form, entregue em 03-01).
- ENT-06: logo do Header sem layout shift (width/height + loading/decoding); build estatico leve.
- Mantidos: 4 temas, responsividade sem overflow, zero hex.
</success_criteria>

<output>
Create `.planning/phases/03-convers-o-qualidade-entrega/03-02-SUMMARY.md` when done
</output>
